{
  "name": "@laserware/arcade",
  "version": "2.3.0",
  "description": "Miscellaneous utility functions.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "repository": {
    "url": "git+https://github.com/laserware/arcade.git",
    "type": "git"
  },
  "author": "Mike Rourke <mike.w.rourke@gmail.com>",
  "license": "MIT",
  "type": "module",
  "files": [
    "dist"
  ],
  "keywords": [
    "utilities"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "rm -rf dist && tsc --project tsconfig.build.json",
    "prepublishOnly": "npm run build",
    "preversion": "pnpm eslint && pnpm typecheck",
    "version": "pnpm prettier && git add -A src",
    "postversion": "git push && git push --tags",
    "typecheck": "tsc --noEmit",
    "prettier": "prettier --cache --write src",
    "eslint": "eslint --cache src",
    "vitest": "vitest"
  },
  "dependencies": {
    "@types/lodash.isequal": "^4.5.8",
    "lodash.isequal": "^4.5.0"
  },
  "devDependencies": {
    "@laserware/eslint-config": "^2.0.0",
    "@rushstack/eslint-patch": "^1.7.2",
    "@types/node": "^20.11.19",
    "@vitest/coverage-istanbul": "^1.3.0",
    "eslint": "^8.56.0",
    "prettier": "^3.2.5",
    "typescript": "^5.3.3",
    "vitest": "^1.3.0"
  },
  "prettier": {
    "trailingComma": "all"
  }
}
